services:
  netwatch-backend:
    environment:
      NETWATCH_ENV: prod

  netwatch-portal:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "${NETWATCH_PORTAL_PORT:-8080}:80"
    environment: {}
    volumes: []
